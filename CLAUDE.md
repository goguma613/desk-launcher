# 바탕화면 런처 위젯

Tauri 2 + Vue 3 Windows 데스크톱 위젯. 기능은 구현이 끝난 상태이고, 남은 작업은 확정된 디자인 시안을 입히는 것입니다.

## 디자인

UI 스펙은 `docs/design-handoff.md`를 따릅니다. 색 · 간격 · 모서리 · 그림자 · 모션 · 상태 규칙이 전부 거기 값으로 정리되어 있습니다.

- 값을 컴포넌트에 직접 쓰지 말고 `src/style.css`의 CSS 변수를 참조할 것
- 색은 rgb 채널만 변수에 담고 alpha는 사용처에서 붙이는 기존 규칙을 유지할 것 — `rgb(var(--tile) / var(--tile-alpha))`
- 시안 토큰 이름과 현재 변수의 매핑, 그리고 코드와 시안이 어긋나는 지점은 `docs/design-handoff.md`의 "이 저장소에 적용하기"에 표로 있습니다

## 시안을 입힐 때 건드리는 곳

| 파일 | 바꾸는 것 |
| --- | --- |
| `src/style.css` | 색 · 간격 · 모서리 · 그림자 · 폰트 토큰 |
| `src/icons.js` | 기본 아이콘 세트 SVG path |
| `src/layout.js` | 프리셋별 치수 상수 |
| `src/components/ItemGrid.vue` | 타일 내부 구성 (아이콘 크기 · 이름 줄 수) |

`layout.js`의 치수 상수와 `ItemGrid.vue`의 `.tile` / `.name` 값은 한 세트입니다. 한쪽만 바꾸면 창 높이가 어긋나 마지막 행이 잘립니다.

## 실행

```bash
npm run tauri dev
```

**반드시 일반 권한 터미널에서.** 관리자 권한으로 띄우면 앱도 관리자 권한이 되고, 일반 권한 탐색기에서 드래그앤드롭이 막힙니다. Windows UIPI 정책이라 코드로 우회할 수 없습니다.

## 범위가 바뀐 것

exe · 바로가기의 **아이콘 자동 추출을 구현 범위에 포함**하기로 했습니다. `README.md`의 "아는 제약" 첫 항목은 아직 옛 내용이니 구현 후 정리해야 합니다.

구현 방법(크레이트 선택, 알파 채널 · 캐시 · .lnk 함정)은 `docs/design-handoff.md`의 "아이콘 처리"에 적어두었습니다.
